#include "main.h"

/**
 * main:check if c is uppercase
 *
 *@c:input for alphabet
 *
 * Return: 1 if uppercase 0 if not
 */

int _isupper(int c);
{
	if(c >= 65 && c <=90)
		Return(1);
	else
		Return(0);
}
